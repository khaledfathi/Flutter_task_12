import 'package:flutter/material.dart';

class MainFavoritesScreen extends StatelessWidget {
  static const String route = 'main/favorites'; 
  const MainFavoritesScreen
({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Favorites Screen'),); 
  }
}