import 'package:flutter/material.dart';

class MainBagScreen extends StatelessWidget {
  static const String route = 'main/bag'; 
  const MainBagScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bag Screen'),); 
  }
}